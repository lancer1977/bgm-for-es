# Dev Studio Notes

This repository is tracked as an internal-tool style Python service/package.

Required readiness checks:

- Project purpose and setup documented in `README.md`.
- Local validation entry point at `scripts/validate.sh`.
- CI workflow runs the local validation entry point.
